let names: string[] = ["GulamM", "Yasir", "Saad"];

console.log(names[0]);
console.log(names[1]);
console.log(names[2]);

// with for loop

for (let i = 0; i < names.length; i++) {
  const element = names[i];
}
